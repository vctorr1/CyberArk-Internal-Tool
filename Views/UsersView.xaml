<UserControl x:Class="CyberArkManager.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="18,14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <md:PackIcon Kind="AccountGroup" Width="22" Height="22" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>
            <TextBlock Text="Usuarios y Grupos" FontSize="18" FontWeight="Bold" Foreground="White" Margin="8,0,0,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Users.Count, StringFormat=' · {0} usuarios'}" FontSize="12" Foreground="#44446A" Margin="6,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="360"/></Grid.ColumnDefinitions>

            <!-- Users list + Groups list -->
            <Grid Grid.Column="0" Margin="0,0,10,0">
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="180"/></Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="130"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" md:HintAssist.Hint="Buscar usuario..." Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="34" Margin="0,0,8,0" FontSize="11"/>
                    <ComboBox Grid.Column="1" ItemsSource="{Binding UserTypes}" SelectedItem="{Binding FilterType}" md:HintAssist.Hint="Tipo" Style="{StaticResource MaterialDesignOutlinedComboBox}" Height="34" Margin="0,0,8,0" FontSize="11"/>
                    <Button Grid.Column="2" Command="{Binding LoadCommand}" Height="34" FontSize="11" Margin="0,0,6,0" Background="{StaticResource AccentBrush}" BorderBrush="{StaticResource AccentBrush}" Style="{StaticResource MaterialDesignRaisedButton}">
                        <StackPanel Orientation="Horizontal"><md:PackIcon Kind="Refresh" Width="14" Height="14" VerticalAlignment="Center"/><TextBlock Text=" Cargar" VerticalAlignment="Center"/></StackPanel>
                    </Button>
                    <Button Grid.Column="3" Command="{Binding LoadGroupsCommand}" Height="34" FontSize="11" Style="{StaticResource MaterialDesignOutlinedButton}">
                        <StackPanel Orientation="Horizontal"><md:PackIcon Kind="AccountMultiple" Width="14" Height="14" VerticalAlignment="Center"/><TextBlock Text=" Grupos" VerticalAlignment="Center"/></StackPanel>
                    </Button>
                </Grid>

                <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,8">
                    <DataGrid ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}"
                              AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                              RowBackground="#1A1A2E" AlternatingRowBackground="#16162A"
                              BorderThickness="0" Style="{StaticResource MaterialDesignDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Usuario"  Binding="{Binding Username}"   Width="*"/>
                            <DataGridTextColumn Header="Nombre"   Binding="{Binding FullName}"   Width="130"/>
                            <DataGridTextColumn Header="Tipo"     Binding="{Binding UserType}"   Width="90"/>
                            <DataGridTextColumn Header="Fuente"   Binding="{Binding Source}"     Width="80"/>
                            <DataGridTextColumn Header="Estado"   Binding="{Binding StatusText}" Width="90"/>
                            <DataGridTextColumn Header="Último acceso" Binding="{Binding LastLogin}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <!-- Groups -->
                <Border Grid.Row="2" Style="{StaticResource Card}">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="GRUPOS" FontSize="9" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="10,8,0,4"/>
                        <DataGrid ItemsSource="{Binding Groups}" SelectedItem="{Binding SelectedGroup}"
                                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                                  HeadersVisibility="Column" BorderThickness="0"
                                  Style="{StaticResource MaterialDesignDataGrid}" FontSize="11">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Nombre"   Binding="{Binding Name}"        Width="*"/>
                                <DataGridTextColumn Header="Tipo"     Binding="{Binding GroupType}"   Width="80"/>
                                <DataGridTextColumn Header="Miembros" Binding="{Binding MembersCount}" Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </Border>
            </Grid>

            <!-- Operations -->
            <Border Grid.Column="1" Style="{StaticResource Card}" Padding="0">
                <TabControl Style="{StaticResource MaterialDesignTabControl}">
                    <TabItem Header="Crear usuario">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="14">
                            <StackPanel>
                                <TextBlock Text="NUEVO USUARIO" FontSize="9" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,10"/>
                                <TextBox Text="{Binding NewUsername,  UpdateSourceTrigger=PropertyChanged}" md:HintAssist.Hint="Usuario *"     md:HintAssist.IsFloating="True" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,8" FontSize="12"/>
                                <ComboBox ItemsSource="{Binding UserTypes}" SelectedItem="{Binding NewUserType}" md:HintAssist.Hint="Tipo *" md:HintAssist.IsFloating="True" Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="0,0,0,8" FontSize="12"/>
                                <PasswordBox md:HintAssist.Hint="Contraseña inicial *" md:HintAssist.IsFloating="True" Style="{StaticResource MaterialDesignOutlinedPasswordBox}" Margin="0,0,0,8"/>
                                <TextBox Text="{Binding NewFirstName, UpdateSourceTrigger=PropertyChanged}" md:HintAssist.Hint="Nombre"        md:HintAssist.IsFloating="True" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,8" FontSize="12"/>
                                <TextBox Text="{Binding NewLastName,  UpdateSourceTrigger=PropertyChanged}" md:HintAssist.Hint="Apellidos"     md:HintAssist.IsFloating="True" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,8" FontSize="12"/>
                                <TextBox Text="{Binding NewEmail,     UpdateSourceTrigger=PropertyChanged}" md:HintAssist.Hint="Correo electrónico"         md:HintAssist.IsFloating="True" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,12" FontSize="12"/>
                                <Button Command="{Binding CreateCommand}" Height="36" FontSize="11" Background="{StaticResource AccentBrush}" BorderBrush="{StaticResource AccentBrush}" Style="{StaticResource MaterialDesignRaisedButton}">
                                    <StackPanel Orientation="Horizontal"><md:PackIcon Kind="AccountPlus" Width="14" Height="14" VerticalAlignment="Center"/><TextBlock Text=" Crear usuario" VerticalAlignment="Center"/></StackPanel>
                                </Button>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <TabItem Header="Gestionar">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="14">
                            <StackPanel>
                                <TextBlock Text="ACCIONES SOBRE USUARIO SELECCIONADO" FontSize="9" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,12"/>
                                <WrapPanel Margin="0,0,0,12">
                                    <Button Command="{Binding ActivateCommand}" Height="34" FontSize="10" Margin="0,0,6,6" Background="{StaticResource SuccessBrush}" BorderBrush="{StaticResource SuccessBrush}" Style="{StaticResource MaterialDesignRaisedButton}">
                                        <StackPanel Orientation="Horizontal"><md:PackIcon Kind="AccountCheck" Width="13" Height="13" VerticalAlignment="Center"/><TextBlock Text=" Activar" VerticalAlignment="Center"/></StackPanel>
                                    </Button>
                                    <Button Command="{Binding SuspendCommand}" Height="34" FontSize="10" Margin="0,0,6,6" Foreground="{StaticResource WarnBrush}" BorderBrush="{StaticResource WarnBrush}" Style="{StaticResource MaterialDesignOutlinedButton}">
                                        <StackPanel Orientation="Horizontal"><md:PackIcon Kind="AccountOff" Width="13" Height="13" VerticalAlignment="Center"/><TextBlock Text=" Suspender" VerticalAlignment="Center"/></StackPanel>
                                    </Button>
                                    <Button Command="{Binding DeleteCommand}" Height="34" FontSize="10" Margin="0,0,0,6" Foreground="{StaticResource DangerBrush}" BorderBrush="{StaticResource DangerBrush}" Style="{StaticResource MaterialDesignOutlinedButton}">
                                        <StackPanel Orientation="Horizontal"><md:PackIcon Kind="AccountRemove" Width="13" Height="13" VerticalAlignment="Center"/><TextBlock Text=" Eliminar" VerticalAlignment="Center"/></StackPanel>
                                    </Button>
                                </WrapPanel>

                                <Separator Background="#1A1A40" Margin="0,0,0,12"/>
                                <TextBlock Text="RESETEAR CONTRASEÑA" FontSize="9" Foreground="#44446A" FontWeight="Bold" Margin="0,0,0,6"/>
                                <PasswordBox x:Name="ResetUserPasswordBox"
                                             md:HintAssist.Hint="Nueva contraseña"
                                             md:HintAssist.IsFloating="True"
                                             Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                                             PasswordChanged="ResetUserPasswordChanged"
                                             Margin="0,0,0,8" FontSize="12"/>
                                <Button Command="{Binding ResetPasswordCommand}" Height="34" FontSize="11" Margin="0,0,0,16" Style="{StaticResource MaterialDesignOutlinedButton}">
                                    <StackPanel Orientation="Horizontal"><md:PackIcon Kind="LockReset" Width="13" Height="13" VerticalAlignment="Center"/><TextBlock Text=" Restablecer contraseña" VerticalAlignment="Center"/></StackPanel>
                                </Button>

                                <Separator Background="#1A1A40" Margin="0,0,0,12"/>
                                <TextBlock Text="AÑADIR A GRUPO" FontSize="9" Foreground="#44446A" FontWeight="Bold" Margin="0,0,0,6"/>
                                <TextBlock Text="(Selecciona un grupo de la lista inferior)" FontSize="10" Foreground="#33334A" Margin="0,0,0,8"/>
                                <Button Command="{Binding AddToGroupCommand}" Height="34" FontSize="11" Style="{StaticResource MaterialDesignOutlinedButton}">
                                    <StackPanel Orientation="Horizontal"><md:PackIcon Kind="AccountMultiplePlus" Width="13" Height="13" VerticalAlignment="Center"/><TextBlock Text=" Añadir al Grupo Seleccionado" VerticalAlignment="Center"/></StackPanel>
                                </Button>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>
                </TabControl>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Margin="0,6,0,0" FontSize="11"
                   Foreground="{Binding HasError, Converter={StaticResource BoolToColor}, ConverterParameter='#F44336|#4CAF50'}"
                   Visibility="{Binding StatusMessage, Converter={StaticResource NullToVis}}"/>
    </Grid>
</UserControl>

