<UserControl x:Class="CyberArkManager.Views.DiscoveredAccountsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="18,14">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <md:PackIcon Kind="Magnify" Width="22" Height="22" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>
            <TextBlock Text="Cuentas descubiertas pendientes" FontSize="18" FontWeight="Bold" Foreground="White" Margin="8,0,0,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Accounts.Count, StringFormat=' · {0} cuentas'}" FontSize="12" Foreground="#44446A" Margin="6,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="300"/></Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,0,10,0">
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource Card}" Padding="12,8" Margin="0,0,0,10">
                    <WrapPanel>
                        <TextBox Text="{Binding FilterKeyword, UpdateSourceTrigger=PropertyChanged}" md:HintAssist.Hint="Buscar..." Style="{StaticResource MaterialDesignOutlinedTextBox}" Width="200" Height="34" Margin="0,0,8,0" FontSize="11"/>
                        <TextBox Text="{Binding FilterType, UpdateSourceTrigger=PropertyChanged}" md:HintAssist.Hint="Tipo de cuenta" Style="{StaticResource MaterialDesignOutlinedTextBox}" Width="130" Height="34" Margin="0,0,8,0" FontSize="11"/>
                        <Button Command="{Binding LoadCommand}" Height="34" FontSize="11" Margin="0,0,8,0" Background="{StaticResource AccentBrush}" BorderBrush="{StaticResource AccentBrush}" Style="{StaticResource MaterialDesignRaisedButton}">
                            <StackPanel Orientation="Horizontal"><md:PackIcon Kind="Refresh" Width="14" Height="14" VerticalAlignment="Center"/><TextBlock Text=" Cargar" VerticalAlignment="Center"/></StackPanel>
                        </Button>
                        <Button Command="{Binding DeleteCommand}" Height="34" FontSize="11" Foreground="{StaticResource DangerBrush}" BorderBrush="{StaticResource DangerBrush}" Style="{StaticResource MaterialDesignOutlinedButton}">
                            <StackPanel Orientation="Horizontal"><md:PackIcon Kind="Delete" Width="14" Height="14" VerticalAlignment="Center"/><TextBlock Text=" Descartar" VerticalAlignment="Center"/></StackPanel>
                        </Button>
                    </WrapPanel>
                </Border>

                <Border Grid.Row="1" Style="{StaticResource Card}">
                    <DataGrid ItemsSource="{Binding Accounts}" SelectedItem="{Binding SelectedAccount}"
                              AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                              RowBackground="#1A1A2E" AlternatingRowBackground="#16162A"
                              BorderThickness="0" Style="{StaticResource MaterialDesignDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Equipo"      Binding="{Binding ComputerName}"      Width="160"/>
                            <DataGridTextColumn Header="Usuario"     Binding="{Binding UserName}"          Width="130"/>
                            <DataGridTextColumn Header="Tipo"        Binding="{Binding AccountType}"       Width="100"/>
                            <DataGridCheckBoxColumn Header="Admin"   Binding="{Binding Privileged}"        Width="55"/>
                            <DataGridTextColumn Header="SO"          Binding="{Binding OsVersion}"         Width="120"/>
                            <DataGridTextColumn Header="Plataforma"  Binding="{Binding PlatformType}"      Width="100"/>
                            <DataGridTextColumn Header="Descubierto" Binding="{Binding DiscoveryDisplay}"  Width="130"/>
                            <DataGridTextColumn Header="Último logon" Binding="{Binding LastLogonDisplay}" Width="110"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>

            <!-- Onboard panel -->
            <Border Grid.Column="1" Style="{StaticResource Card}" Padding="14">
                <StackPanel>
                    <TextBlock Text="INCORPORAR AL VAULT" FontSize="9" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,10"/>
                    <TextBlock Text="Selecciona una cuenta de la tabla y configura el Safe y Plataforma de destino." FontSize="11" Foreground="#44446A" TextWrapping="Wrap" Margin="0,0,0,14"/>
                    <TextBox Text="{Binding OnboardSafe, UpdateSourceTrigger=PropertyChanged}" md:HintAssist.Hint="Safe destino *" md:HintAssist.IsFloating="True" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,10" FontSize="12"/>
                    <TextBox Text="{Binding OnboardPlat, UpdateSourceTrigger=PropertyChanged}" md:HintAssist.Hint="ID de plataforma *"  md:HintAssist.IsFloating="True" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,14" FontSize="12"/>
                    <Button Command="{Binding OnboardCommand}" Height="38" FontSize="12" Background="{StaticResource SuccessBrush}" BorderBrush="{StaticResource SuccessBrush}" Style="{StaticResource MaterialDesignRaisedButton}">
                        <StackPanel Orientation="Horizontal"><md:PackIcon Kind="ArrowRightCircle" Width="16" Height="16" VerticalAlignment="Center"/><TextBlock Text=" Incorporar cuenta" VerticalAlignment="Center"/></StackPanel>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Margin="0,6,0,0" FontSize="11"
                   Foreground="{Binding HasError, Converter={StaticResource BoolToColor}, ConverterParameter='#F44336|#4CAF50'}"
                   Visibility="{Binding StatusMessage, Converter={StaticResource NullToVis}}"/>
    </Grid>
</UserControl>

