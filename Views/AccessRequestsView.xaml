<UserControl x:Class="CyberArkManager.Views.AccessRequestsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="18,14">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <md:PackIcon Kind="ClipboardCheck" Width="22" Height="22" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>
            <TextBlock Text="Solicitudes de Acceso (Dual Control)" FontSize="18" FontWeight="Bold" Foreground="White" Margin="8,0,0,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Requests.Count, StringFormat=' · {0} solicitudes'}" FontSize="12" Foreground="#44446A" Margin="6,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="300"/></Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,0,10,0">
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <Button Grid.Row="0" Command="{Binding LoadCommand}" Height="34" FontSize="11" Margin="0,0,0,10" Background="{StaticResource AccentBrush}" BorderBrush="{StaticResource AccentBrush}" Style="{StaticResource MaterialDesignRaisedButton}" HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal"><md:PackIcon Kind="Refresh" Width="14" Height="14" VerticalAlignment="Center"/><TextBlock Text=" Cargar mis solicitudes" VerticalAlignment="Center"/></StackPanel>
                </Button>
                <Border Grid.Row="1" Style="{StaticResource Card}">
                    <DataGrid ItemsSource="{Binding Requests}" SelectedItem="{Binding SelectedRequest}"
                              AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                              RowBackground="#1A1A2E" AlternatingRowBackground="#16162A"
                              BorderThickness="0" Style="{StaticResource MaterialDesignDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Solicitante"   Binding="{Binding RequestorUserName}" Width="130"/>
                            <DataGridTextColumn Header="Safe"        Binding="{Binding SafeName}"          Width="130"/>
                            <DataGridTextColumn Header="Operación"   Binding="{Binding Operation}"         Width="100"/>
                            <DataGridTextColumn Header="Estado"      Binding="{Binding StatusTitle}"       Width="100"/>
                            <DataGridTextColumn Header="Motivo"      Binding="{Binding RequestorReason}"   Width="*"/>
                            <DataGridTextColumn Header="Expira"      Binding="{Binding ExpirationDisplay}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>

            <!-- Approve/Reject panel -->
            <Border Grid.Column="1" Style="{StaticResource Card}" Padding="14">
                <StackPanel>
                    <TextBlock Text="RESPONDER SOLICITUD" FontSize="9" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,10"/>
                    <TextBlock Text="Selecciona una solicitud de la tabla para confirmar o rechazar." FontSize="11" Foreground="#44446A" TextWrapping="Wrap" Margin="0,0,0,14"/>
                    <TextBox Text="{Binding Reason, UpdateSourceTrigger=PropertyChanged}"
                             md:HintAssist.Hint="Motivo / Justificación"
                             md:HintAssist.IsFloating="True"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="0,0,0,14" FontSize="12" AcceptsReturn="True" Height="80" TextWrapping="Wrap"/>
                    <Button Command="{Binding ConfirmCommand}" Height="38" FontSize="12" Margin="0,0,0,8" Background="{StaticResource SuccessBrush}" BorderBrush="{StaticResource SuccessBrush}" Style="{StaticResource MaterialDesignRaisedButton}">
                        <StackPanel Orientation="Horizontal"><md:PackIcon Kind="CheckCircle" Width="16" Height="16" VerticalAlignment="Center"/><TextBlock Text=" Confirmar / Aprobar" VerticalAlignment="Center"/></StackPanel>
                    </Button>
                    <Button Command="{Binding RejectCommand}" Height="38" FontSize="12" Foreground="{StaticResource DangerBrush}" BorderBrush="{StaticResource DangerBrush}" Style="{StaticResource MaterialDesignOutlinedButton}">
                        <StackPanel Orientation="Horizontal"><md:PackIcon Kind="CloseCircle" Width="16" Height="16" VerticalAlignment="Center"/><TextBlock Text=" Rechazar" VerticalAlignment="Center"/></StackPanel>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Margin="0,6,0,0" FontSize="11"
                   Foreground="{Binding HasError, Converter={StaticResource BoolToColor}, ConverterParameter='#F44336|#4CAF50'}"
                   Visibility="{Binding StatusMessage, Converter={StaticResource NullToVis}}"/>
    </Grid>
</UserControl>

