<UserControl x:Class="CyberArkManager.Views.SystemHealthView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="18,14">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <md:PackIcon Kind="HeartPulse" Width="22" Height="22" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>
            <TextBlock Text="Salud del Sistema" FontSize="18" FontWeight="Bold" Foreground="White" Margin="8,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="12,8" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding LoadCommand}" Height="34" FontSize="11" Background="{StaticResource AccentBrush}" BorderBrush="{StaticResource AccentBrush}" Style="{StaticResource MaterialDesignRaisedButton}">
                    <StackPanel Orientation="Horizontal"><md:PackIcon Kind="Refresh" Width="14" Height="14" VerticalAlignment="Center"/><TextBlock Text=" Verificar Estado de Componentes" VerticalAlignment="Center"/></StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Components}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource Card}" Margin="0,0,0,10" Padding="0">
                            <Expander IsExpanded="True">
                                <Expander.Header>
                                    <StackPanel Orientation="Horizontal" Margin="4,0">
                                        <md:PackIcon Kind="Server" Width="16" Height="16" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding ComponentName}" FontSize="13" FontWeight="SemiBold" Foreground="White" Margin="8,0,0,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding ComponentVersion, StringFormat=' v{0}'}" FontSize="11" Foreground="#44446A" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding Instances.Count, StringFormat=' · {0} instancias'}" FontSize="11" Foreground="#44446A" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Expander.Header>
                                <DataGrid ItemsSource="{Binding Instances}"
                                          AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                                          HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                                          RowBackground="#16162A" BorderThickness="0"
                                          Style="{StaticResource MaterialDesignDataGrid}" FontSize="11" Margin="8,0,8,8">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="IP / Host"     Binding="{Binding IP}"                Width="160"/>
                                        <DataGridTextColumn Header="Usuario"       Binding="{Binding ComponentUserName}" Width="160"/>
                                        <DataGridTextColumn Header="Estado"        Binding="{Binding StatusText}"       Width="120"/>
                                        <DataGridCheckBoxColumn Header="Conectado" Binding="{Binding Connected}"        Width="80"/>
                                        <DataGridTextColumn Header="Último logon"  Binding="{Binding LastLogon}"        Width="*"/>
                                    </DataGrid.Columns>
                                    <DataGrid.RowStyle>
                                        <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Connected}" Value="False">
                                                    <Setter Property="Foreground" Value="#F44336"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGrid.RowStyle>
                                </DataGrid>
                            </Expander>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Margin="0,6,0,0" FontSize="11"
                   Foreground="{Binding HasError, Converter={StaticResource BoolToColor}, ConverterParameter='#F44336|#4CAF50'}"
                   Visibility="{Binding StatusMessage, Converter={StaticResource NullToVis}}"/>
    </Grid>
</UserControl>
