<Window x:Class="CyberArkManager.Views.CsvPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Height="700"
        Width="1200"
        MinHeight="520"
        MinWidth="900"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource ContentBg}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#10101C" CornerRadius="8" Padding="14" Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="{Binding RowCount, StringFormat=' - {0} filas'}" FontSize="12" Foreground="#9FE8F2" Margin="8,4,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="#10101C" CornerRadius="8">
            <DataGrid ItemsSource="{Binding Rows}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      EnableRowVirtualization="True"
                      EnableColumnVirtualization="True"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      ScrollViewer.CanContentScroll="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      RowBackground="#1A1A2E"
                      AlternatingRowBackground="#16162A"
                      BorderThickness="0"
                      Style="{StaticResource MaterialDesignDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Binding="{Binding RowNumber}" Width="48"/>
                    <DataGridTextColumn Header="username" Binding="{Binding UserName}" Width="140"/>
                    <DataGridTextColumn Header="address" Binding="{Binding Address}" Width="180"/>
                    <DataGridTextColumn Header="safe" Binding="{Binding SafeName}" Width="140"/>
                    <DataGridTextColumn Header="platformID" Binding="{Binding PlatformId}" Width="140"/>
                    <DataGridTextColumn Header="password" Binding="{Binding Password}" Width="120"/>
                    <DataGridCheckBoxColumn Header="EnableAutoMgmt" Binding="{Binding AutoManagement}" Width="110"/>
                    <DataGridTextColumn Header="ManualMgmtReason" Binding="{Binding ManualReason}" Width="170"/>
                    <DataGridCheckBoxColumn Header="UseSudoOnReconcile" Binding="{Binding UseSudoOnReconcile}" Width="140"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Window>


