<UserControl x:Class="CyberArkManager.Views.ApplicationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="18,14">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <md:PackIcon Kind="Application" Width="22" Height="22" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>
            <TextBlock Text="Aplicaciones AAM / AIM" FontSize="18" FontWeight="Bold" Foreground="White" Margin="8,0,0,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Apps.Count, StringFormat=' · {0} aplicaciones'}" FontSize="12" Foreground="#44446A" Margin="6,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="340"/></Grid.ColumnDefinitions>

            <!-- Left: apps list + auth methods -->
            <Grid Grid.Column="0" Margin="0,0,10,0">
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="180"/></Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" md:HintAssist.Hint="Filtrar por ID de aplicación..." Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="34" Margin="0,0,8,0" FontSize="11"/>
                    <Button Grid.Column="1" Command="{Binding LoadCommand}" Height="34" FontSize="11" Margin="0,0,8,0" Background="{StaticResource AccentBrush}" BorderBrush="{StaticResource AccentBrush}" Style="{StaticResource MaterialDesignRaisedButton}">
                        <StackPanel Orientation="Horizontal"><md:PackIcon Kind="Refresh" Width="14" Height="14" VerticalAlignment="Center"/><TextBlock Text=" Cargar" VerticalAlignment="Center"/></StackPanel>
                    </Button>
                    <Button Grid.Column="2" Command="{Binding LoadMethodsCommand}" Height="34" FontSize="11" Style="{StaticResource MaterialDesignOutlinedButton}">
                        <StackPanel Orientation="Horizontal"><md:PackIcon Kind="Key" Width="14" Height="14" VerticalAlignment="Center"/><TextBlock Text=" Métodos" VerticalAlignment="Center"/></StackPanel>
                    </Button>
                </Grid>

                <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,8">
                    <DataGrid ItemsSource="{Binding Apps}" SelectedItem="{Binding SelectedApp}"
                              AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                              HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                              RowBackground="#1A1A2E" AlternatingRowBackground="#16162A"
                              BorderThickness="0" Style="{StaticResource MaterialDesignDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID de aplicación" Binding="{Binding AppID}" Width="*"/>
                            <DataGridTextColumn Header="Propietario" Binding="{Binding OwnerName}"   Width="130"/>
                            <DataGridTextColumn Header="Correo electrónico" Binding="{Binding BusinessOwnerEmail}" Width="160"/>
                            <DataGridTextColumn Header="Estado"      Binding="{Binding StatusText}"  Width="80"/>
                            <DataGridTextColumn Header="Expiración"  Binding="{Binding ExpirationDate}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <Border Grid.Row="2" Style="{StaticResource Card}">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="MÉTODOS DE AUTENTICACIÓN" FontSize="9" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="10,8,0,4"/>
                        <DataGrid ItemsSource="{Binding Methods}" SelectedIndex="{Binding SelectedMethodIndex}"
                                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                                  HeadersVisibility="Column" BorderThickness="0"
                                  Style="{StaticResource MaterialDesignDataGrid}" FontSize="11">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Tipo"   Binding="{Binding AuthType}"  Width="120"/>
                                <DataGridTextColumn Header="Valor"  Binding="{Binding AuthValue}" Width="*"/>
                                <DataGridTextColumn Header="Comentario" Binding="{Binding Comment}"  Width="120"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </Border>
            </Grid>

            <!-- Right: operations -->
            <Border Grid.Column="1" Style="{StaticResource Card}" Padding="14">
                <StackPanel>
                    <TextBlock Text="AÑADIR MÉTODO DE AUTENTICACIÓN" FontSize="9" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,10"/>
                    <ComboBox ItemsSource="{Binding MethodTypes}" SelectedItem="{Binding NewMethodType}" md:HintAssist.Hint="Tipo *" md:HintAssist.IsFloating="True" Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="0,0,0,8" FontSize="12"/>
                    <TextBox Text="{Binding NewMethodValue, UpdateSourceTrigger=PropertyChanged}" md:HintAssist.Hint="Valor *" md:HintAssist.IsFloating="True" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,12" FontSize="12"/>
                    <WrapPanel Margin="0,0,0,16">
                        <Button Command="{Binding AddMethodCommand}" Height="34" FontSize="11" Margin="0,0,6,0" Background="{StaticResource AccentBrush}" BorderBrush="{StaticResource AccentBrush}" Style="{StaticResource MaterialDesignRaisedButton}">
                            <StackPanel Orientation="Horizontal"><md:PackIcon Kind="Plus" Width="13" Height="13" VerticalAlignment="Center"/><TextBlock Text=" Añadir" VerticalAlignment="Center"/></StackPanel>
                        </Button>
                        <Button Command="{Binding DeleteMethodCommand}" Height="34" FontSize="11" Foreground="{StaticResource DangerBrush}" BorderBrush="{StaticResource DangerBrush}" Style="{StaticResource MaterialDesignOutlinedButton}">
                            <StackPanel Orientation="Horizontal"><md:PackIcon Kind="Delete" Width="13" Height="13" VerticalAlignment="Center"/><TextBlock Text=" Quitar selección" VerticalAlignment="Center"/></StackPanel>
                        </Button>
                    </WrapPanel>
                    <Separator Background="#1A1A40" Margin="0,0,0,12"/>
                        <TextBlock Text="ZONA PELIGROSA" FontSize="9" FontWeight="Bold" Foreground="{StaticResource DangerBrush}" Margin="0,0,0,8"/>
                        <Button Command="{Binding DeleteCommand}" Height="34" FontSize="11" Foreground="{StaticResource DangerBrush}" BorderBrush="{StaticResource DangerBrush}" Style="{StaticResource MaterialDesignOutlinedButton}">
                        <StackPanel Orientation="Horizontal"><md:PackIcon Kind="Delete" Width="13" Height="13" VerticalAlignment="Center"/><TextBlock Text=" Eliminar aplicación" VerticalAlignment="Center"/></StackPanel>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Margin="0,6,0,0" FontSize="11"
                   Foreground="{Binding HasError, Converter={StaticResource BoolToColor}, ConverterParameter='#F44336|#4CAF50'}"
                   Visibility="{Binding StatusMessage, Converter={StaticResource NullToVis}}"/>
    </Grid>
</UserControl>

