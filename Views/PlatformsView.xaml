<UserControl x:Class="CyberArkManager.Views.PlatformsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="18,14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <md:PackIcon Kind="Cog" Width="22" Height="22" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>
            <TextBlock Text="Plataformas" FontSize="18" FontWeight="Bold" Foreground="White" Margin="8,0,0,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Platforms.Count, StringFormat=' · {0} plataformas'}" FontSize="12" Foreground="#44446A" Margin="6,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Filters + Actions -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="12,8" Margin="0,0,0,10">
            <WrapPanel>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" md:HintAssist.Hint="Buscar plataforma..." Style="{StaticResource MaterialDesignOutlinedTextBox}" Width="220" Height="34" Margin="0,0,8,0" FontSize="11"/>
                <Button Command="{Binding LoadCommand}" Height="34" FontSize="11" Margin="0,0,8,0" Background="{StaticResource AccentBrush}" BorderBrush="{StaticResource AccentBrush}" Style="{StaticResource MaterialDesignRaisedButton}">
                    <StackPanel Orientation="Horizontal"><md:PackIcon Kind="Refresh" Width="14" Height="14" VerticalAlignment="Center"/><TextBlock Text=" Cargar" VerticalAlignment="Center"/></StackPanel>
                </Button>
                <Button Command="{Binding ActivateCommand}"   Height="34" FontSize="11" Margin="0,0,8,0" Style="{StaticResource MaterialDesignOutlinedButton}"><StackPanel Orientation="Horizontal"><md:PackIcon Kind="CheckCircle" Width="13" Height="13" VerticalAlignment="Center"/><TextBlock Text=" Activar" VerticalAlignment="Center"/></StackPanel></Button>
                <Button Command="{Binding DeactivateCommand}" Height="34" FontSize="11" Margin="0,0,8,0" Style="{StaticResource MaterialDesignOutlinedButton}"><StackPanel Orientation="Horizontal"><md:PackIcon Kind="Cancel" Width="13" Height="13" VerticalAlignment="Center"/><TextBlock Text=" Desactivar" VerticalAlignment="Center"/></StackPanel></Button>
                <TextBox Text="{Binding DuplicateName, UpdateSourceTrigger=PropertyChanged}" md:HintAssist.Hint="Nombre copia" Style="{StaticResource MaterialDesignOutlinedTextBox}" Width="150" Height="34" Margin="0,0,8,0" FontSize="11"/>
                <Button Command="{Binding DuplicateCommand}" Height="34" FontSize="11" Margin="0,0,8,0" Style="{StaticResource MaterialDesignOutlinedButton}"><StackPanel Orientation="Horizontal"><md:PackIcon Kind="ContentCopy" Width="13" Height="13" VerticalAlignment="Center"/><TextBlock Text=" Duplicar" VerticalAlignment="Center"/></StackPanel></Button>
                <Button Command="{Binding ExportCommand}"    Height="34" FontSize="11" Margin="0,0,8,0" Style="{StaticResource MaterialDesignOutlinedButton}"><StackPanel Orientation="Horizontal"><md:PackIcon Kind="Download" Width="13" Height="13" VerticalAlignment="Center"/><TextBlock Text=" Exportar ZIP" VerticalAlignment="Center"/></StackPanel></Button>
                <Button Command="{Binding ImportCommand}"    Height="34" FontSize="11" Margin="0,0,8,0" Style="{StaticResource MaterialDesignOutlinedButton}"><StackPanel Orientation="Horizontal"><md:PackIcon Kind="Upload" Width="13" Height="13" VerticalAlignment="Center"/><TextBlock Text=" Importar ZIP" VerticalAlignment="Center"/></StackPanel></Button>
                <Button Command="{Binding DeleteCommand}"    Height="34" FontSize="11" Foreground="{StaticResource DangerBrush}" BorderBrush="{StaticResource DangerBrush}" Style="{StaticResource MaterialDesignOutlinedButton}"><md:PackIcon Kind="Delete" Width="13" Height="13"/></Button>
            </WrapPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}">
            <DataGrid ItemsSource="{Binding Platforms}" SelectedItem="{Binding SelectedPlatform}"
                      AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      RowBackground="#1A1A2E" AlternatingRowBackground="#16162A"
                      BorderThickness="0" Style="{StaticResource MaterialDesignDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"          Binding="{Binding Id}"           Width="160"/>
                    <DataGridTextColumn Header="Nombre"      Binding="{Binding Name}"         Width="*"/>
                    <DataGridTextColumn Header="Tipo"        Binding="{Binding SystemType}"   Width="100"/>
                    <DataGridTextColumn Header="Plat. Base"  Binding="{Binding PlatformBaseID}" Width="130"/>
                    <DataGridTextColumn Header="Estado"      Binding="{Binding ActiveText}"   Width="80"/>
                    <DataGridTextColumn Header="Descripción" Binding="{Binding Description}"  Width="200"/>
                </DataGrid.Columns>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Active}" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
        </Border>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Margin="0,6,0,0" FontSize="11"
                   Foreground="{Binding HasError, Converter={StaticResource BoolToColor}, ConverterParameter='#F44336|#4CAF50'}"
                   Visibility="{Binding StatusMessage, Converter={StaticResource NullToVis}}"/>
    </Grid>
</UserControl>
