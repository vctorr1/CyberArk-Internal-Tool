<UserControl x:Class="CyberArkManager.Views.PsmRecordingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="18,14">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <md:PackIcon Kind="RecordCircle" Width="22" Height="22" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>
            <TextBlock Text="Grabaciones PSM" FontSize="18" FontWeight="Bold" Foreground="White" Margin="8,0,0,0" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding Recordings.Count, StringFormat=' · {0} grabaciones'}" FontSize="12" Foreground="#44446A" Margin="6,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="12,8" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding FilterSafe, UpdateSourceTrigger=PropertyChanged}" md:HintAssist.Hint="Filtrar por Safe" Style="{StaticResource MaterialDesignOutlinedTextBox}" Width="200" Height="34" Margin="0,0,8,0" FontSize="11"/>
                <Button Command="{Binding LoadCommand}" Height="34" FontSize="11" Background="{StaticResource AccentBrush}" BorderBrush="{StaticResource AccentBrush}" Style="{StaticResource MaterialDesignRaisedButton}">
                    <StackPanel Orientation="Horizontal"><md:PackIcon Kind="Refresh" Width="14" Height="14" VerticalAlignment="Center"/><TextBlock Text=" Cargar Grabaciones" VerticalAlignment="Center"/></StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}">
            <DataGrid ItemsSource="{Binding Recordings}" SelectedItem="{Binding SelectedRecording}"
                      AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      RowBackground="#1A1A2E" AlternatingRowBackground="#16162A"
                      BorderThickness="0" Style="{StaticResource MaterialDesignDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID Sesión"  Binding="{Binding SessionID}"     Width="180"/>
                    <DataGridTextColumn Header="Usuario"    Binding="{Binding User}"          Width="130"/>
                    <DataGridTextColumn Header="Máquina"    Binding="{Binding RemoteMachine}" Width="160"/>
                    <DataGridTextColumn Header="Safe"       Binding="{Binding SafeName}"      Width="130"/>
                    <DataGridTextColumn Header="Fichero"    Binding="{Binding FileName}"      Width="160"/>
                    <DataGridTextColumn Header="Inicio"     Binding="{Binding StartDisplay}"  Width="130"/>
                    <DataGridTextColumn Header="Duración"   Binding="{Binding DurationDisplay}" Width="90"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Margin="0,6,0,0" FontSize="11"
                   Foreground="{Binding HasError, Converter={StaticResource BoolToColor}, ConverterParameter='#F44336|#4CAF50'}"
                   Visibility="{Binding StatusMessage, Converter={StaticResource NullToVis}}"/>
    </Grid>
</UserControl>
